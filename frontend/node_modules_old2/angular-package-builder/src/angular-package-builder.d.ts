import { AngularPackage } from './angular-package';
export declare class AngularPackageBuilder {
    static package(angularPackage: AngularPackage): Promise<void>;
    private static transform;
    private static compile;
    private static bundle;
    private static compose;
    private static cleanupOutputFolder;
    private static cleanupTemporaryOutputFolder;
}
