import { SourceFile } from 'ts-simple-ast';
import { AngularPackage } from '../angular-package';
export declare class AngularPackageTransformer {
    readonly sourceFiles: Array<SourceFile>;
    private readonly angularPackage;
    constructor(angularPackage: AngularPackage);
    transform(): Promise<void>;
    private inlineExternalTemplates;
    private inlineExternalStyles;
    private convertLineBreaks;
    private save;
    private handleExternalTemplateError;
    private handleExternalStyleError;
}
