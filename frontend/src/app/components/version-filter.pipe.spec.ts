import { VersionFilterPipe } from './version-filter.pipe';

describe('VersionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new VersionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
