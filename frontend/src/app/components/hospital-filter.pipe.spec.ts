import { HospitalFilterPipe } from './hospital-filter.pipe';

describe('HospitalFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HospitalFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
